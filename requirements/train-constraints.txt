# AtlasOps training-stack constraints (empirically tested set)
#
# The [train] extra in pyproject.toml intentionally declares floor-only
# minimums for scope flexibility, but SFT behaviour depends on exact
# trainer/template semantics (assistant_only_loss generation masks,
# apply_chat_template tools handling). Install training environments with:
#
#   pip install -e .[train] -c requirements/train-constraints.txt --no-deps
#   # plus the pinned transitive core listed below as needed
#
# or constrain an existing environment with -c. These are the EXACT versions
# the SFT contract tests (tests/test_sft_template_wiring.py,
# tests/test_sft_mask_proof.py) were validated against on Windows/CPython 3.12.
#
# Tokenizer provenance used by the mask-proof tests (no weights):
#   repo:     Qwen/Qwen2.5-7B-Instruct
#   revision: a09a35458c702b33eeacc393d103063234e8bc28
#   files:    tokenizer.json, tokenizer_config.json, vocab.json, merges.txt
#
torch==2.7.1
transformers==4.57.6
trl==0.19.1
peft==0.17.1
datasets==4.8.5
accelerate==1.14.0
tokenizers==0.22.2
huggingface_hub==0.36.2
